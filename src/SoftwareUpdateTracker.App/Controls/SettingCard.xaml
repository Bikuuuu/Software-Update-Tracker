<UserControl
    x:Class="SoftwareUpdateTracker.App.Controls.SettingCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource Card}" Padding="14,10">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Normal" />
                <VisualState x:Name="Disabled">
                    <VisualState.Setters>
                        <Setter Target="HeaderText.Foreground" Value="{ThemeResource TextFillColorDisabledBrush}" />
                        <Setter Target="DescriptionText.Foreground" Value="{ThemeResource TextFillColorDisabledBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="HeaderText" TextWrapping="Wrap" />
                <TextBlock x:Name="DescriptionText" Style="{StaticResource Secondary}" TextWrapping="Wrap" Visibility="Collapsed" />
            </StackPanel>
            <ContentPresenter x:Name="Host" Grid.Column="1" VerticalAlignment="Center" />
        </Grid>
    </Border>
</UserControl>

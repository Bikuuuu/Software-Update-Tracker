<Window
    x:Class="SoftwareUpdateTracker.App.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Software Update Tracker">
    <Grid x:Name="Root" Padding="16,14,16,16" RowSpacing="10" Opacity="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.RenderTransform>
            <TranslateTransform x:Name="Slide" />
        </Grid.RenderTransform>
        <StackPanel Orientation="Horizontal" Spacing="10">
            <Image x:Name="Logo" Width="22" Height="22" />
            <TextBlock Text="Software Update Tracker" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Flyout spike: acrylic, rounded corners, instant show." Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap" />
        <ProgressBar x:Name="Busy" Grid.Row="2" IsIndeterminate="False" />
    </Grid>
</Window>
